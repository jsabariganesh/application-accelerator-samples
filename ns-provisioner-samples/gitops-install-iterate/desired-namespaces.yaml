#@data/values

#@ def get_ns():
#@     small = [{"item": "where-for-dinner-small-" + str(i)} for i in range(1, 301)]
#@     return small
#@ end

---
namespaces: #@ get_ns()

#! namespaces: #@ [{"name": "where-for-dinner-small-" + str(i)} for i in range(1, 301)
